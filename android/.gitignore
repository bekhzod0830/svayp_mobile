gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
# Shared debug keystore is deliberately committed: every developer must sign
# debug builds with the same key, because its fingerprint is listed in
# web.svaypai.com/.well-known/assetlinks.json (App Links → /pay/return).
!/debug.keystore
